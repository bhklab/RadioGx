.linearQuadratic        Linear quadratic
Cleveland_small         Cleaveland_mut RadioSet subsetted
RadioSet                RadioSet constructor
RadioSet-class          A Class to Contain RadioGenomic datasets
                        together with their curations
[,RadioSet,ANY,ANY,ANY-method
                        '['
checkRSetStructure      A function to verify the structure of a
                        RadioSet
computeAUC              computeAUC: computes AUC
computeD10              Compute D10
computeSF2              Compute SF2
dim,RadioSet-method     Get the dimensions of a RadioSet
doseResponseCurve       Plot drug response curve of a given drug and a
                        given cell for a list of rSets (objects of the
                        RadioSet class).
linearQuadraticModel    Fit linear-quadratic curves to dose-response
                        data
mDataNames,RadioSet-method
                        mDataNames
plotCurve               Plot radiation dose-response curve
radSensitivitySig       Creates a signature representing the
                        association between gene expression (or other
                        molecular profile) and radiation dose response,
                        for use in radiation sensitivity analysis.
radiationInfo           radiationInfo Generic
radiationInfo<-         radiationInfo<- Generic
radiationTypes          radiationTypes Generic
radiationTypes<-        radiationTypes<- Generic
show,RadioSet-method    Show a RadioSet
show,RadioSig-method    Show RadioGx Signatures
showSigAnnot            Show the Annotations of a signature object
subsetTo                A function to subset a RadioSet to data
                        containing only specified radiations, cells and
                        genes
summarizeMolecularProfiles
                        Takes molecular data from a PharmacoSet, and
                        summarises them into one entry per drug
summarizeSensitivityProfiles
                        Takes the sensitivity data from a RadioSet, and
                        summarises them into a drug vs cell line table
